#!/usr/bin/env node

var _ = require('lodash');
var tamagoya = require('../lib/tamagoya');

tamagoya(function (err, menus) {
  if (err) { return console.error(err); }
  _.each(menus, function (m) { console.log(m); });
});

// vim: se et ts=2 sw=2 sts=2 ft=javascript :
